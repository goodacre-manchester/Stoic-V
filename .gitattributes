# Shell scripts run under WSL bash — must stay LF even when checked out on Windows.
*.sh text eol=lf
# RISCOF/Verilog/firmware sources: normalize to LF for tool consistency.
*.ld text eol=lf
*.S text eol=lf
*.s text eol=lf
